// components/shared/Icon.tsx — Native stub for Icon.
// Native builds don't use lucide-react. This renders nothing.
// When native is built out, replace with lucide-react-native.

export default function Icon(_props: {
  name: string;
  size?: number;
  color?: string;
  className?: string;
  style?: object;
}) {
  return null;
}
